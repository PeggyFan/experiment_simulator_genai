# Requirements for experiment_simulator_genai
# Detected third-party imports in the repository: numpy, pandas, scipy, matplotlib, streamlit
# Assumption: Python >= 3.8
numpy>=1.21
pandas>=1.3
scipy>=1.7
matplotlib>=3.4
streamlit>=1.10

# Optional: pin exact versions after validation in your environment, e.g. use pip freeze > requirements.txt